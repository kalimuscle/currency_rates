# Proyecto MERN-GraphQL

This is an example project using MERN (MongoDB, Express, React, Node.js) to query the Exchange Rate API https://v6.exchangerate-api.com

## Instalación

To install the project dependencies, run the following command:
1. docker-compose up
2. cd server && npm install
3. npm run dev
4. cd client && npm install
5. npm run start
